<div class="main-content col flex-fill">
  <iframe src="<%= url_for(@collection.site) %>" frameBorder="0"></iframe>
</div>
<div class="main-sidebar col flex-fill">
  <div class="main-sidebar-scroller">
    <%= render "shared/branding"%>
    <%= render 'form' %>
  </div>
  <%= link_to site_collections_path(@collection.site), class: "action" do %>
    <i class="fa fa-arrow-left"></i>
  <% end %>
</div>
